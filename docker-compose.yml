version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - AUTH_SECRET=${AUTH_SECRET:-change-me-in-production}
      - DEBUG=false
